# 光阴砚 AEONQUILL — 开发者文档

> **文档梗概**：说明光阴砚 AEONQUILL 当前无限画布、本地图像/视频桥接、目录、状态、运行方式、验证路径和维护限制。
>
> **具体规划法则**：
>
> 1. `App.canvasDocument` 是画布状态的权威来源；正式图片和视频任务状态以本地 `JobStore` 为权威，由 `JobScheduler` 分配 CPU/GPU 资源，再经 SSE 合并到前端。
> 2. 屏幕坐标与世界坐标必须通过相机换算；高频手势只更新内存预览，结束时经统一命令校验并形成一次文档修订与撤销快照。
> 3. 图片浏览器草稿与正式处理必须区分；正式去背景/超分由桥接调用本机 rembg/Real-ESRGAN，H3 视频由固定模板交给独立 ComfyUI。
> 4. 浏览器不得直接调用 ComfyUI，也不得提交任意工作流；视频参数必须经过服务端白名单和版本化模板。
> 5. `.runtime/projects/` 是正式单机项目存储（SQLite + 内容寻址资产）；`.runtime/inputs`、`.runtime/assets` 和 `jobs.json` 仍是执行期数据；`config/local.json` 是机器私有配置，不得提交。
> 6. 正式任务必须登记资源类别、优先级、幂等键和超时并经过统一调度器；RTX 4060 上 GPU 容量固定为 1，禁止绕过调度并发。
> 7. 本地桥接只允许回环绑定；除首个 HTML/健康握手外，`/api` 必须同时通过 Host、Origin/Fetch Metadata 与随机 HttpOnly 会话校验。
> 8. 正式画布写入必须通过 `CanvasCommand` 或已注册的画布工具；Agent 不得替换整份文档、修改锁定节点或写入媒体 URL/任务字段。
> 9. 画布显示受管图片时必须使用版本化缩略图/预览图；原始内容资产只供正式编辑、导出、哈希和版本追踪，不得为离屏节点直接解码。
> 10. 桌面壳只负责随机回环端口、桥接生命周期、窗口沙箱和发行；Electron/Tauri 不得分叉业务 API、项目格式或任务实现。
> 11. `.miaohui`、`MIAOHUI_*` 与旧本地存储键是兼容标识；在迁移工具、旧项目回归和回滚路径完成前不得做破坏性改名。
>
> **最后一次更新时间**：2026-08-14
>
> **更新者**：项目统筹与产品规划组

---

## 0. 当前基线与硬规则

`package.json` 当前声明 `0.1.0`。项目是由 React 单页面、本机 Node 桥接、持久化资源调度器、受控图片处理子进程和按需运行的独立 ComfyUI 组成的单机创作软件原型。画布、空间索引/视口裁剪、分级图片预览、浏览器草稿、本机 FFmpeg/rembg/Real-ESRGAN、像素编辑、固定 MiniMax H3 Turbo 文生/图生工作流、真实任务进度、幂等/优先级/超时/恢复、不可变输出版本、本地资产与画布回填均已形成可运行闭环；Electron/Tauri 同核桌面实验、自包含 Node 22 sidecar 和未签名 NSIS 安装卸载也已通过本机验证。

当前没有云端多租户服务、账号支付、云端生产数据库、内容审核、桌面代码签名或自动更新/回滚。项目已于 `V0.1.0` 初始化 Git，`main` 绑定 GitHub private 仓库 `falling-feather/AeonQuill`；更早的 `FE-001`、`DEV-001`、`DEV-002`、`DEV-003`、`QA-001`、`SEC-001`、`ARCH-001`、`DATA-001`、`BE-001`、`PERF-001` 与 `REL-001` 仍只作为首次快照中的既有基线，不伪造独立提交。任务原件见[02-项目规划.md 第 6 节](02-项目规划.md#6-正式任务安排)。

当前开发进入 `V0.2.x` 原型完善周期，正在推进统一恢复体验、语义局部编辑、像素资产往返和智能视频受控队列；范围与完成口径见[23-V0.2原型完善验收方案.md](02-子文档/23-V0.2原型完善验收方案.md)。本文只在各任务形成稳定实现后更新对应路径、接口与维护方式。

必须遵守：

1. 导入图片和派生图片可在提交前短暂使用 Data URL；SQLite 自动保存会将其外置为 SHA-256 内容寻址文件并改写成 `/api/project-assets/<hash>`，正式项目不得长期依赖内嵌二进制。
2. `pixel` 元件保存像素索引数组，禁止把每个像素创建为独立画布元件。
3. `connector` 通过 `fromId`、`toId` 引用端点；删除节点时必须同步清理连接线。
4. `processingStack` 是派生来源与显示开关元数据，不是完整图像 DAG；正式处理结果通过 `assetVersionId`、`jobId` 和 `sourceElementId` 追踪。
5. 视频二进制不进入画布 JSON；执行期可短暂引用 `/api/assets/...`，项目自动保存后必须规范化为 `/api/project-assets/<sha256>` 和不可变版本元数据。
6. ComfyUI 只监听 `127.0.0.1:8188`；浏览器只访问 `127.0.0.1:8787` 的 AEONQUILL 桥接服务。
7. 当前运行中取消调用 ComfyUI 全局 interrupt，只能用于受控单机，不能直接扩展到多租户。
8. rembg 只允许本地 `u2netp`/`u2net` 白名单并校验权重 MD5；禁止在首个用户任务中静默联网下载模型。
9. AEONQUILL 只关闭自己启动的 ComfyUI 进程；检测到外部 ComfyUI 时只提示显存风险，不得代用户终止。

## 目录

1. [系统定位](#1-系统定位)
2. [总体架构](#2-总体架构)
3. [技术栈](#3-技术栈)
4. [目录结构](#4-目录结构)
5. [启动与运行流程](#5-启动与运行流程)
6. [模块实现](#6-模块实现)
7. [接口、数据与状态](#7-接口数据与状态)
8. [页面与响应式](#8-页面与响应式)
9. [配置与环境](#9-配置与环境)
10. [开发操作](#10-开发操作)
11. [扩展指南](#11-扩展指南)
12. [测试与验证](#12-测试与验证)
13. [维护与故障排查](#13-维护与故障排查)
14. [关联文档](#14-关联文档)

## 1. 系统定位

AEONQUILL 当前验证一个高集成视觉创作产品的单机形态：用户在无限画布中组织图片、像素画、文本和视频，通过浏览器确定性处理器完成低成本编辑，通过本机 ComfyUI 完成 H3 视频生成。

| 术语 | 当前含义 |
| --- | --- |
| 视口 | `.canvas-viewport`，接收指针、滚轮和文件拖放 |
| 世界层 | `.canvas-world`，统一应用相机平移与缩放 |
| 相机 | `Camera { x, y, zoom }` |
| 元件 | `CanvasElement`；当前包含图片、像素、文本、视频和连接线等种类 |
| 浏览器图片草稿 | `ImageLab` 内的短任务，刷新后不恢复 |
| 正式图片/视频任务 | 本地桥接持久化 Job，刷新后通过 REST/SSE 恢复 |
| 执行器 | FFmpeg、rembg/u2netp、Real-ESRGAN NCNN 和回环地址上的 ComfyUI |
| 本地资产 | `.runtime/assets/*.{png,mp4}`，通过同源 API 提供；MP4 支持 Range |

本原型已负责单机视频推理和异步任务，但不负责用户登录、云端保存、多租户隔离、多人协作或商业计费。

## 2. 总体架构

```text
index.html
└── React App
    ├── CanvasDocument / selection / document undo history
    ├── CanvasCommand / ToolDefinition / Draft
    ├── CanvasWorkspace
    │   ├── CanvasElementView（含 video）
    │   ├── SelectionToolbar
    │   ├── 空间索引 / 视口裁剪
    │   └── Canvas MiniMap
    ├── Inspector
    │   ├── 编辑
    │   ├── VideoStudio
    │   ├── 图层
    │   └── 任务 / 日志 / 错误 / 重试
    ├── ImageLab ──> browser imageProcessing / imageApi
    ├── PixelEditor
    └── videoApi ──> REST + SSE

Electron 主进程 / Tauri Rust 主进程（桌面模式）
└── 随机回环端口 + 沙箱 WebView + 受控桥接生命周期
    └── server/index.mjs / 自包含 Node 22 sidecar

server/launcher.mjs（浏览器/开发模式）
└── server/index.mjs 127.0.0.1:8787（常驻）
    ├── image-processor（FFmpeg、rembg 与 Real-ESRGAN 适配）
    ├── security（随机会话、来源/路径/日志/子进程/响应策略边界）
    ├── job-scheduler（CPU/GPU 容量、优先级、取消、超时、恢复与资源事件）
    ├── runtime-manager（按需启动/复用/空闲关闭 ComfyUI 127.0.0.1:8188）
    ├── workflow-builder（固定模板 + 受控参数）
    ├── comfy-client（HTTP + WebSocket + history）
    ├── JobStore（.runtime/jobs.json + SSE）
    ├── ProjectStore（SQLite WAL + SHA-256 资产 + .miaohui 项目包）
    ├── AssetPreviewService（版本化 WebP 缩略/预览缓存）
    ├── 错误归一化、取消、快速重试
    ├── .runtime/inputs、assets、models 与 tools
    └── dist 静态站点与 MP4 Range 服务
```

画布和本地任务是两个不同的权威域：`App.canvasDocument` 负责空间、元件、schemaVersion 与修订历史，`JobStore` 负责持久化执行事实，`JobScheduler` 只负责资源分配与生命周期推进。SSE 到达后，视频 Job 更新占位节点；完成的图片 Job 创建新的图片节点与连接线，原图不覆盖。系统回填仍经过文档校验但不污染用户撤销栈；用户撤销画布操作不会取消执行任务，任务取消也不会回滚其他画布操作。

## 3. 技术栈

| 层级 | 技术 | 用途 |
| --- | --- | --- |
| UI | React 18、TypeScript 5、Vite 6、Lucide | 画布、面板、像素编辑和任务交互 |
| 样式 | 原生 CSS | 设计令牌、画布节点、状态卡和响应式抽屉 |
| 浏览器图像 | Canvas 2D、ImageData | 裁剪、浅色去背草稿、插值、K-means、Bayer 和首帧规范化 |
| 本地服务 | Node.js 22 内建 HTTP、Fetch、WebSocket、FormData | API、SSE、任务、工作流、输出下载和静态托管 |
| 本地图像执行 | FFmpeg/FFprobe、rembg 2.0.66 + u2netp、Real-ESRGAN NCNN Vulkan | 确定性图像处理、主体分割与模型超分 |
| 视频执行 | ComfyUI 0.30.2、MiniMax H3、Larry Turbo LoRA | 文生/图生 AV 采样、解码与 MP4 |
| 当前持久化与调度 | Node `sqlite`、SHA-256 文件资产、原子 `.runtime/jobs.json`、`JobScheduler`、`localStorage` 恢复副本 | 单机项目/版本/来源、CPU/GPU Job 与执行资产；不是云端多租户数据库/队列 |
| 桌面实验 | Electron 43.4.0；Tauri 2.11.4、Rust 1.97.1、WebView2、NSIS、自包含 Node 22 sidecar | 同核封装、渲染器沙箱、桥接进程树、安装卸载与可逆选型；尚无签名/更新 |
| 质量基线 | Node Test Runner、Playwright + 系统 Edge、Chromium Performance 指标 | 单元/离线工作流、隔离 API、桌面与移动端浏览器烟测及固定大画布基准 |

前端没有外部状态库或路由。桥接服务没有额外 npm 运行依赖，使用 Node 22 的原生 WebSocket 和 Fetch。

## 4. 目录结构

```text
pic/
├── config/
│   ├── local.example.json          # 可分发配置模板
│   └── local.json                  # 当前机器配置，Git 忽略
├── design/
│   ├── infinite-canvas-workbench-v2-final.png
│   ├── image-lab-state-v2.png
│   ├── video-generation-workbench-v3.png
│   ├── video-generation-error-v3.png
│   ├── image-enhancement-center-v5.png
│   └── video-director-runtime-v4.png
├── doc/
│   ├── 00-项目总纲.md
│   ├── 01-开发者文档.md
│   ├── 01-子文档/11-本地H3视频工作流.md
│   ├── 01-子文档/12-无限画布性能与资源分级.md
│   ├── 01-子文档/13-桌面封装与发行架构.md
│   ├── 02-项目规划.md
│   └── 03-开发历史.md
├── desktop/
│   ├── shared/bridge-contract.mjs  # 两壳共享的回环端口、健康/关闭与路径契约
│   ├── electron/main.mjs           # Electron 沙箱窗口与 Utility Process 生命周期
│   ├── sidecar.pkg.json            # 自包含 Node 22 sidecar 收集清单
│   └── tauri/src-tauri/            # Rust 主进程、capability、图标与 NSIS 配置
├── server/
│   ├── index.mjs                   # 本地 API 与静态托管
│   ├── image-processor.mjs         # 图片能力探测、参数白名单与子进程执行
│   ├── asset-preview-service.mjs   # 版本化缩略/预览缓存、并发去重与原子生成
│   ├── rembg-runner.py             # 仅本地权重的受控 rembg 入口
│   ├── job-scheduler.mjs           # 持久化 CPU/GPU 资源调度、容量前检与费用事件
│   ├── validate-image-tools.mjs    # 四种确定性处理器真实验证
│   ├── launcher.mjs                # 常驻桥接统一启动
│   ├── runtime-manager.mjs         # ComfyUI 生命周期与进程归属
│   ├── runtime-paths.mjs           # 项目根目录与可隔离运行时目录
│   ├── security.mjs                # 会话、来源、安全响应头、路径和脱敏
│   ├── comfy-client.mjs            # ComfyUI 适配
│   ├── job-store.mjs               # Job 持久化与事件
│   ├── project-store.mjs           # SQLite 项目、内容寻址资产、版本、GC 与项目包
│   ├── workflow-builder.mjs        # 模板参数注入
│   ├── validate-workflows.mjs      # 实时契约验证
│   └── workflows/
│       ├── minimax-h3-t2v.json
│       └── minimax-h3-i2v.json
├── src/
│   ├── App.tsx                     # 画布与任务组合入口
│   ├── types.ts                    # 元件、Job、运行时类型
│   ├── lib/
│   │   ├── imageProcessing.ts
│   │   ├── imageApi.ts
│   │   ├── assetVariants.ts
│   │   ├── canvasSpatialIndex.mjs
│   │   ├── canvasSpatialIndex.d.mts
│   │   ├── canvasCore.mjs
│   │   ├── canvasCore.d.ts
│   │   ├── projectApi.ts
│   │   └── videoApi.ts
│   └── components/
│       ├── CanvasWorkspace.tsx
│       ├── CanvasElementView.tsx
│       ├── Inspector.tsx
│       ├── VideoStudio.tsx
│       ├── ImageLab.tsx
│       └── PixelEditor.tsx
├── scripts/
│   ├── benchmark-canvas.mjs        # 真实 4K 固定夹具与桌面/窄屏性能断言
│   ├── benchmark-desktop-shells.mjs# Electron/Tauri 三轮交替资源基准
│   ├── build-node-sidecar.mjs      # 构建 Tauri 自包含 Node 22 桥接
│   ├── package-electron.mjs        # 构建最小 Electron 便携目录
│   ├── tauri-toolchain.ps1         # 加载项目 Rust + MSVC 并检查/构建/封装
│   ├── validate-desktop-*.mjs      # 两壳、sidecar、安装卸载专项门禁
│   ├── qa-runtime.mjs              # 随机端口、临时运行时与安全进程清理
│   ├── validate-api.mjs            # 隔离 API/SSE/资产/Range 契约
│   ├── validate-browser.mjs        # 桌面与移动端系统浏览器烟测
│   └── validate.mjs                # quick/baseline/live 分层入口与报告
├── tests/
│   ├── asset-preview-service.test.mjs
│   ├── canvas-spatial-index.test.mjs
│   ├── desktop-bridge-contract.test.mjs
│   ├── image-request.test.mjs
│   ├── job-store.test.mjs
│   ├── job-scheduler.test.mjs
│   ├── canvas-core.test.mjs
│   ├── project-store.test.mjs
│   └── workflow-builder.test.mjs
├── .runtime/                       # 项目 SQLite/不可变资产、Job、输入、模型、工具与日志，Git 忽略
├── package.json
└── vite.config.ts
```

`dist/`、`node_modules/`、`.runtime/`、`desktop/tauri/src-tauri/target/` 和 Tauri sidecar 二进制是构建或运行产物，不是源码修改入口。

## 5. 启动与运行流程

### 5.1 前端初始化

1. `src/main.tsx` 挂载 `App`。
2. `loadProject()` 先构造 v8/v7 本地恢复候选；随后读取 `/api/projects/current`，采用 revision 不低于本地候选的 SQLite 项目，均不存在时回退到示例画布。
3. `App` 拉取 `/api/jobs` 和 `/api/runtime/status`，并订阅 `/api/jobs/events`。
4. 视频 Job 快照与本地图片 Job 合并到 `App.jobs`；项目 revision 变化后自动保存并接收规范化资产引用。
5. 画布中带 `jobId` 的图片/视频元件按 Job 状态恢复运行、失败或完成呈现。

### 5.2 视频任务

1. 用户在“视频”页选择文生或图生、镜头运动、动作强度、连贯性、声音、约束、画幅、时长、流水线、音频和 Seed。
2. 图生模式从当前图片元件读取首帧，并可上传末帧；浏览器把两者 cover 裁切到目标宽高并导出 PNG Data URL。
3. `POST /api/jobs/video` 只接受白名单参数，并用 `Idempotency-Key` 绑定请求；服务端原子写入持久化 Job 与 GPU 调度元数据。
4. `JobScheduler` 按优先级取得唯一 GPU 容量并设置两小时安全超时；Runtime Manager 再根据 `persistent | idle | manual` 策略准备 ComfyUI，只有妙绘创建的子进程允许自动关闭。
5. `workflow-builder` 克隆相应模板，注入首/末帧和受控参数；关闭音频时移除无用分支。
6. 桥接先建立 ComfyUI WebSocket，再 `/prompt` 提交任务。
7. 节点执行与 `progress.value/max` 转为准备、条件、采样、解码、封装和保存状态。
8. 完成后桥接从 history 找到 `SaveVideo` 输出，通过 `/view` 下载到临时 MP4 并原子改名；720P 交付档再由可取消 FFmpeg 子进程生成临时文件并提交。
9. 桥接计算最终文件 SHA-256 和确定性 `outputVersion` 后才把 Job 置为 `completed`；SSE 回填画布，项目保存将执行期 URL 外置为不可变项目资产。调度器记录实际 GPU 毫秒，队列清空后 `idle` 策略进入关闭倒计时。

完整节点和 API 契约见[本地 H3 视频工作流](01-子文档/11-本地H3视频工作流.md)。

### 5.3 画布与图片任务

- 用户离散修改通过能力注册表编译为 `CanvasCommand`，再由纯函数执行器校验、递增 revision 并记录完整文档历史；旧复杂调用暂由内部 `elements.replace` 适配器渐进迁移，Agent 无此权限。
- 拖动和缩放用 `onElementsPreview()` 连续更新内存预览，不校验、不落盘、不增加 revision；指针释放后才通过 `commitCanvasPreview()` 校验并提交一次文档修订。
- 图片实验室应用调整时更新同一节点；浏览器裁剪/草稿和正式图片 Job 都创建派生图片与连接线；像素化草稿可创建可编辑像素节点。
- 正式操作先读取 `GET /api/image-tools`；`POST /api/jobs/image` 只接受操作与参数白名单，并要求前端为一次用户提交生成稳定 `Idempotency-Key`。`masked-adjust` 额外要求独立的 PNG/JPEG/WebP Mask Data URL，并把源图与蒙版分别写入受管输入目录；输入不会进入公开 Job，输出先写临时 PNG，完成后作为 `.runtime/assets` 正式执行资产。
- 语义图片操作先读取 `GET /api/semantic-workflows`。注册表固定登记元素提取、区域编辑、定点修改和人像调整四类能力，并分别报告 `ready`、`runtime-stopped`、`missing-dependency`、`node-mismatch` 或 `template-pending`；当前只有 `element-extract@impact-sam-v1` 开放执行。
- 元素提取在图片上记录 0–1 归一化的正/负点击点，经 `POST /api/jobs/semantic-image` 进入共享 GPU 调度。执行器按需启动 ComfyUI、调用 Impact Pack 的 `/sam/prepare` 与 `/sam/detect`，再由 FFmpeg 把蒙版合成为透明 PNG；透明元素和原始灰度蒙版都以独立资产 URL 返回，原图不覆盖。
- 区域调整只接受由 `element-extract`、`semantic-element-extract`、`mask-refine`、`remove-background` 或 `alpha-cleanup` 明确产生的 Alpha 派生图。桥接以源图和派生图 Alpha 执行 `selection-highlight`、`background-dim` 或 `background-blur`，强度 0.1–1、羽化 0–32px；生成式 `region-edit` 在模板与质量门禁未完成时继续显示真实 `template-pending`，不开放执行。
- 五种 FFmpeg 操作登记为 CPU 资源，模型去背景/超分和 H3 视频登记为 GPU 资源；CPU 容量由 `MIAOHUI_IMAGE_CONCURRENCY` 控制，GPU 容量固定为 1。各资源内部按优先级、排队时间和稳定序号选择任务，不再维护图片专用队列。
- GPU 图片任务发现妙绘托管且空闲的 ComfyUI 时会先停止其进程树；外部 ComfyUI 只记录警告，不会自动关闭。
- 完成 Job 经 SSE 到达 `App` 后，以 `job.id` 去重，生成包含 `processingStack`、`assetVersionId`、`sourceElementId` 的派生图片；输出哈希与版本 ID 已由桥接确定，用户删除后不会因其他 Job 更新而复活。
- 只有 `CanvasDocument.revision` 变化后才以 260ms 防抖写入恢复副本并提交 `ProjectStore`；ProjectStore 会外置 Data URL 与运行时 PNG/MP4、返回规范化引用，相机平移/缩放和手势中间帧不制造整树持久化。
- `CanvasWorkspace` 为可见非连接线节点建立旋转 AABB 均匀网格索引；相机只查询带 240 屏幕像素 overscan 的世界视口，选中节点和背景强制保留，连接线通过端点 Map 和包围盒裁剪。相机高频更新合并到动画帧，小地图用单 Canvas 绘制全部缩略几何。
- 受管图片在画布内按屏幕投影选择 `thumbnail-v1`（512px）或 `preview-v1`（1600px）WebP；编辑和导出仍读取原始 SHA-256 资产。派生缓存按源哈希并发去重、临时写入后原子提交，不进入资产版本链。

### 5.4 桌面壳与桥接生命周期

Electron 和 Tauri 均先取得随机回环端口，以受限环境启动同一桥接，再等待 `/api/health` 成功后创建窗口。渲染器只加载本次启动的精确 Origin，不拥有 Node、Shell 或文件系统桥；页面业务仍使用现有 HTTP/SSE/HttpOnly 会话，不维护桌面专用状态。

Electron 使用 `utilityProcess.fork(server/index.mjs)`；Tauri 使用 Rust Shell 插件启动自包含 Node 22 sidecar。Tauri 通过 `MIAOHUI_PARENT_CONTROL=stdio` 发送 `shutdown\n`，Electron 通过 Utility Process 生命周期请求退出；两者都验证桥接退出、端口关闭和渲染器沙箱，超时才终止自有进程树。当前发行候选和实测差异见[13-桌面封装与发行架构](01-子文档/13-桌面封装与发行架构.md)。

## 6. 模块实现

| 模块 | 路径 | 当前职责 |
| --- | --- | --- |
| 产品入口 | `src/ProductApp.tsx`、`src/shell/**`、`src/components/Home/**` | 品牌主页、三模式 Hash 导航、真实运行时映射、最近项目与模式可用性；负责进入各模式，不复制领域内核 |
| 均衡模式组合 | `src/App.tsx` | 画布命令、历史、自动保存、Job 合并、视频节点创建和结果回填；由产品入口以 `#/balanced` 挂载 |
| 像素模式 | `src/modes/pixel/**`、`src/lib/pixel/**` | 严格像素文档、确定性命令与逆命令、图层/帧/调色板/洋葱皮、Sprite Sheet RGBA/元数据；产品入口补充本机草稿与 PNG 下载 |
| 智能视频模式 | `src/modes/video/**`、`src/lib/story/**`、`server/intelligent-video/**` | 严格故事项目、确定性场景/镜头编译、受控 workflow ID 与任务 DAG；首轮仅生成计划，不调用外部 LLM 或执行模型 |
| 画布内核 | `src/lib/canvasCore.mjs`、`src/lib/canvasCore.d.ts` | 版本化文档、迁移、严格校验、原子命令、工具注册、草稿预览/提交与 actor 权限 |
| 画布交互 | `src/components/CanvasWorkspace.tsx`、`src/lib/canvasSpatialIndex.mjs` | 相机、世界坐标、帧合并手势、空间索引、视口/连接线裁剪、Canvas 小地图和图片拖放 |
| 元件展示 | `src/components/CanvasElementView.tsx`、`src/lib/assetVariants.ts` | 图片资源分级、像素、文本、视频运行/错误/完成态与播放器 |
| 视频工作台 | `src/components/VideoStudio.tsx` | 模式、导演控制、首末帧、显存档位、Comfy 生命周期和当前任务阶段 |
| 属性与任务 | `src/components/Inspector.tsx` | 编辑、视频、图层、任务；日志、错误建议、取消、重试和参数复用 |
| 视频前端协议 | `src/lib/videoApi.ts` | REST、SSE 与目标画幅首帧规范化 |
| 图片实验室 | `src/components/ImageLab.tsx`、`src/components/SemanticPointEditor.tsx` | 十类图像模式、草稿/正式执行切换、SAM 正负点击提示、Alpha 派生图门禁、区域效果、能力状态、参数、比较与进度 |
| 图片前端协议 | `src/lib/imageApi.ts` | 能力探测、图片规范化和正式 Job 提交 |
| 浏览器处理器 | `src/lib/imageProcessing.ts` | 确定性草稿与像素量化 |
| 像素编辑 | `src/components/PixelEditor.tsx` | 任意登记尺寸的索引像素编辑 |
| 本地 API | `server/index.mjs` | 参数/幂等/优先级校验、运行时检查、Job、SSE、错误、取消、原子输出和静态站点 |
| 本地桥接安全 | `server/security.mjs` | 回环绑定、随机 HttpOnly 会话、Host/Origin/Fetch Metadata、CSP、路径边界、递归脱敏与子进程环境白名单 |
| 本地项目存储 | `server/project-store.mjs` | SQLite WAL 项目元数据、逻辑资产版本、SHA-256 文件、引用保护 GC、目录包与单文件 `.miaohui` 包 |
| 项目前端协议 | `src/lib/projectApi.ts` | 项目恢复/自动保存、完整项目包下载与校验导入 |
| 通用任务调度 | `server/job-scheduler.mjs` | CPU/GPU 独立容量、优先级、取消、超时、排队恢复、磁盘容量前检和本机资源用量事件 |
| 任务存储 | `server/job-store.mjs` | 原子 JSON 持久化、幂等键冲突保护、运行中断失败与排队任务恢复、SSE 事件来源 |
| 图片处理器 | `server/image-processor.mjs` | 能力探测、尺寸/参数上限、五种 FFmpeg 操作、双输入蒙版合成、rembg/Real-ESRGAN 子进程 |
| 语义工作流注册表 | `server/semantic-workflow-registry.mjs` | 四类稳定工作流 ID、依赖/模型/节点探测、参数与资源档位；只开放已实现适配器 |
| 图片预览缓存 | `server/asset-preview-service.mjs` | 512/1600px 版本化 WebP、单并发生成、同键去重、原子提交与 GC 清理 |
| 画布性能基准 | `scripts/benchmark-canvas.mjs` | 真实 4K 固定夹具、构建指纹、桌面/窄屏 DOM、帧、指针反馈与资源层级断言 |
| 桌面共享契约 | `desktop/shared/bridge-contract.mjs` | 随机回环端口、健康/关闭等待与隔离路径校验 |
| Electron 壳 | `desktop/electron/main.mjs` | 沙箱 BrowserWindow、Utility Process、来源限制与退出收束 |
| Tauri 壳 | `desktop/tauri/src-tauri/src/lib.rs` | Node 22 sidecar、WebView2 capability、NSIS 生命周期与 QA 报告 |
| 去背景运行器 | `server/rembg-runner.py` | 固定模型白名单、MD5 校验、禁止静默下载后调用 rembg Python API |
| Comfy 适配 | `server/comfy-client.mjs` | 上传、Prompt、WebSocket、history、queue、interrupt 与 view |
| 工作流 | `server/workflow-builder.mjs`、`server/workflows/` | 版本化固定模板和产品预设映射 |
| 启动器 | `server/launcher.mjs` | 启动常驻桥接，不预热 ComfyUI |
| 运行时管理 | `server/runtime-manager.mjs` | 策略、冷启动、健康检查、进程归属、倒计时与安全停止 |

图片实验室同时提供浏览器草稿和本机正式处理。草稿仍可能在主线程运行且只供预览；正式去背景是 `u2netp` 通用主体分割，正式超分是 Real-ESRGAN，并不等于生产级细发丝 Matting 或任意素材的最佳质量。当前适配器仍在桥接进程调度子进程，正式分发前应拆出隔离 Worker，并建立素材基准与许可证清单。

## 7. 接口、数据与状态

### 7.1 本地 API

生产页面的首个 HTML 响应或 `GET /api/health` 会写入进程内随机 `miaohui_session` Cookie（`HttpOnly; SameSite=Strict`）。除健康握手外，全部 `/api` 请求都必须携带该 Cookie，并通过回环 Host 及显式 Origin/Referer/Fetch Metadata 校验；SSE 复用同一 Cookie。服务重启会轮换凭据，前端收到 401 时只允许重新执行一次健康握手。API 不接受 Bearer Token、URL Token 或任意远端 Origin。

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| GET | `/api/health` | 桥接存活并建立/轮换本机页面会话；唯一不要求已有会话的 API |
| GET | `/api/runtime/status` | Comfy、GPU、显存、队列、节点和模型检查 |
| POST | `/api/runtime/start` | 按需启动或复用 ComfyUI |
| POST | `/api/runtime/stop` | 在安全边界内关闭妙绘拥有的空闲进程 |
| POST | `/api/runtime/policy` | 更新运行策略和空闲阈值 |
| GET | `/api/workflows` | 模式与产品预设目录 |
| GET | `/api/image-tools` | 返回图片处理器可用性、执行器、模型、参数与上限；`?refresh=1` 强制探测 |
| GET | `/api/semantic-workflows` | 返回四类语义工作流的版本、成熟度、依赖、节点、资源档位和真实可用状态；不返回本机路径 |
| GET | `/api/scheduler` | 返回 CPU/GPU 容量、活动 Job 与按优先级排列的等待 Job |
| GET | `/api/projects/current?id=...` | 读取 SQLite 项目；首次运行返回 `project: null` |
| PUT | `/api/projects/current` | 原子保存 CanvasDocument，外置媒体并记录资产版本/来源 |
| GET | `/api/projects/current/package?id=...` | 下载 gzip 封装的完整 `.miaohui` 项目与全部版本资产 |
| POST | `/api/projects/import?projectId=...` | 校验包结构、文档 Schema、哈希与版本引用后导入 |
| POST | `/api/projects/gc` | 只回收项目引用和版本引用均为零的内容资产 |
| GET/HEAD | `/api/project-assets/:sha256` | 读取不可变项目原图；支持 ETag、长缓存与 Range；图片可请求 `?variant=thumbnail-v1` 或 `preview-v1` |
| GET | `/api/jobs` | Job 列表 |
| GET | `/api/jobs/events` | SSE 快照与增量更新 |
| POST | `/api/jobs/video` | 创建文生/图生任务；支持 `Idempotency-Key` 与 `X-MiaoHui-Priority` |
| POST | `/api/jobs/image` | 创建受控本机图片任务；仅收 PNG/JPEG/WebP Data URL 与白名单参数，支持幂等键与优先级 |
| POST | `/api/jobs/semantic-image` | 创建受控语义图片任务；当前只接受 `element-extract`、至少一个正向点、最多 32 个总点和 0.5–0.99 阈值 |
| GET | `/api/jobs/:id` | 单任务 |
| POST | `/api/jobs/:id/cancel` | 取消等待或运行任务 |
| POST | `/api/jobs/:id/retry` | 快速预设重试 |
| DELETE | `/api/jobs` | 清理终态任务 |
| GET | `/api/assets/:filename` | PNG 下载和 MP4 Range 播放 |

开发服务器将 `/api` 代理到 8787；生产构建由 8787 同源托管。

### 7.2 核心数据

正式画布使用 `CanvasDocument` schema v1。`localStorage` 键已从 `miaohui-canvas:v7` 升级为 `miaohui-canvas:v8`；启动时依次读取 SQLite、v8 与 v7，并把旧 `{ elements, camera }` 或旧 `version: 2` 导出迁移为当前文档。面向用户的导入/导出改用带文档、版本、provenance 和全部二进制的 `.miaohui` 包；JSON 只再用于内部调试。

```ts
type CanvasDocument = {
  schemaVersion: 1
  id: string
  title: string
  revision: number
  createdAt: number
  updatedAt: number
  elements: CanvasElement[]
  camera: Camera
}

type CanvasCommand = {
  schemaVersion: 1
  id: string
  actor: 'user' | 'tool' | 'agent' | 'system'
  type: 'elements.add' | 'elements.patch' | 'elements.remove' |
    'elements.replace' | 'camera.set' | 'document.reset'
  baseRevision: number
  payload: Record<string, unknown>
}
```

事务在同一 `baseRevision` 上原子执行，成功只增加一次 revision；过期命令/草稿、未知字段、重复 ID、悬空连接线、非法尺寸与超限数组会整体拒绝。Agent 当前只可调用 add/patch/remove，且通用 patch 只能修改视觉与几何白名单，不能写入 `src`、`jobId`、资产或处理来源字段。

`ProjectStore` 将画布逻辑资产 ID 与内容哈希分开：`CanvasElement.assetId` 表示逻辑资产，`assetVersion` 是单调版本号，`assetVersionId` 指向 SQLite `asset_versions`；实际二进制以 SHA-256 为文件名。派生节点可通过 `sourceElementId` 建立跨逻辑资产父版本，来源同时记录 Job、处理操作和原始引用。项目更新先校验 revision/引用/版本冲突，再以临时文件 + SQLite `BEGIN IMMEDIATE` 提交；失败不会留下正式项目记录或孤立暂存文件。

当前实现使用 Node.js 22 内置 `node:sqlite` 并启用 WAL、`synchronous=FULL` 与外键约束。它适合当前单机基线，但仍属于需要固定运行时验证的接口：桌面封装必须捆绑并锁定已回归的 Node 版本，升级前执行项目重开、100 轮保存、导入导出和故障注入测试；云端多租户阶段再评估独立数据库驱动或服务。

`CanvasElement` 继续承载通用几何和内容字段；视频新增：

```ts
type CanvasElement = {
  // 通用 id / kind / name / transform / appearance ...
  videoSrc?: string
  posterSrc?: string
  jobId?: string
  jobStatus?: ProcessingJobStatus
  jobPhase?: VideoJobPhase
  jobProgress?: number
  jobDetail?: string
  jobError?: ProcessingJobError
}
```

`ProcessingJob` 同时容纳本地图片 Job 与视频 Job。图片 `output` 保存 `width`、`height`、`mimeType`、`bytes`、`provider`；视频 `output` 保存 Comfy 文件位置摘要，两者用 `isImageJobOutput()` 区分。`scheduling` 固定记录资源类别、优先级、幂等键、尝试/上限、超时和排队/开始/结束时间；`costEvents` 当前记录金额为 0、不可计费的 CPU/GPU 估算与实际毫秒，为未来云 API 费用事件保留同一结构。`outputVersion` 记录最终 SHA-256、逻辑资产、版本 ID 和 provenance。公开 API 会移除 `inputPath`、`outputPath`、`temporaryOutputPath`、`lastFramePath` 和运行中的进程控制对象。

### 7.3 权威来源

| 状态 | 权威来源 | 持久化 |
| --- | --- | --- |
| 画布与相机 | `App.canvasDocument` / `documentRef` | `ProjectStore` SQLite；`localStorage` schema v1 仅作离线恢复副本 |
| 撤销重做 | `pastRef` / `futureRef` 的 `CanvasDocument` 快照 | 刷新清空 |
| 浏览器图片草稿 | `ImageLab` / `App.jobs` | 刷新清空 |
| 正式图片/视频任务 | `server/JobStore` + `JobScheduler` | 原子 `.runtime/jobs.json`；CPU/GPU 排队状态可重建 |
| 图片/视频输入 | 本机桥接 | `.runtime/inputs` |
| 图片/视频输出 | 本机桥接 | `.runtime/assets` |
| 正式项目资产/版本 | `server/ProjectStore` | `.runtime/projects/projects.sqlite3` + `.runtime/projects/assets/<sha256>.<ext>` |
| Comfy 执行事实 | Comfy queue、WebSocket 与 history | 由 Comfy 管理；桥接同步摘要 |

## 8. 页面与响应式

当前使用无需服务端重写的轻量 Hash 导航：`/` 是产品主页，`#/balanced` 是均衡画布，`#/pixel` 是像素工作台，`#/smart-video` 是智能视频规划台。浏览器前进/后退与各模式返回按钮保持一致；旧书签 `/` 仍有效。

| 区域 | 组件 | 当前状态 |
| --- | --- | --- |
| 产品主页 | `AeonQuillShell` | 品牌、三模式入口、最近项目、真实本机运行时与成熟度说明 |
| 产品路由 | `ProductApp` | Hash 路由、运行时轮询、模式接线、像素草稿与导出宿主 |
| 顶栏 | `TopBar` | 项目、保存、历史、分享、导出 |
| 工具栏 | `ToolRail` | 桌面左侧，移动端底部 |
| 无限画布 | `CanvasWorkspace` | 元件、连接线、选择与视频结果 |
| 快捷指令 | `PromptBar` | 确定性关键词命令，不是 Agent |
| 右侧面板 | `Inspector` | 编辑 / 视频 / 图层 / 任务四页签 |
| 视频页 | `VideoStudio` | 文生/图生、导演控制、首末帧、显存档位、运行策略与当前任务 |
| 图片实验室 | `ImageLab` | 七模式模态比较、能力探测、草稿/正式执行切换、参数与进度 |
| 像素编辑器 | `PixelEditor` | 模态像素绘制 |
| 像素工作台 | `PixelModeWorkbench` | 多层多帧确定性编辑；一次 pointer gesture 对应一次 revision 与一个撤销单元 |
| 智能视频规划台 | `SmartVideoWorkbench` | 灵感/剧本到场景、镜头、连续性资产和受控任务计划；执行按钮在首轮禁用 |

断点：1120px 以下缩窄工具栏和面板；880px 以下右侧面板成为抽屉；640px 以下工具栏移到底部、顶栏简化。产品主页、像素工作台和智能视频规划台均在 390×844 开发自检中无页面级横向溢出；该结果不替代后续独立 QA。

## 9. 配置与环境

| 配置 | 位置或环境变量 | 当前默认 |
| --- | --- | --- |
| 机器配置 | `config/local.json` | 从 example 复制；Git 忽略 |
| Comfy URL | `COMFY_URL` / `comfyUrl` | `http://127.0.0.1:8188` |
| Comfy 根目录 | `COMFY_ROOT` / `comfyRoot` | 机器配置 |
| Python | `COMFY_PYTHON` / `pythonPath` | 机器配置 |
| 运行策略 | `MIAOHUI_COMFY_POLICY` / `comfyLaunchPolicy` | `idle`（按需启动、空闲关闭） |
| 空闲阈值 | `MIAOHUI_COMFY_IDLE_SECONDS` / `comfyIdleSeconds` | `300` 秒 |
| 桥接端口 | `MIAOHUI_PORT` / `bridgePort` | `8787` |
| 桥接主机 | `MIAOHUI_HOST` | `127.0.0.1` |
| 额外开发来源 | `MIAOHUI_ALLOWED_ORIGINS` / `allowedOrigins` | 空；仅允许逗号分隔/数组形式的显式回环 HTTP(S) Origin |
| 配置文件覆盖 | `MIAOHUI_CONFIG` | `config/local.json`；隔离 QA 指向临时空配置 |
| 运行时目录覆盖 | `MIAOHUI_RUNTIME_DIR` | `.runtime`；仅测试/封装隔离时覆盖 |
| FFmpeg | `FFMPEG_PATH` / `imageTools.ffmpegPath` | 当前机器配置 |
| FFprobe | `FFPROBE_PATH` / `imageTools.ffprobePath` | FFmpeg 同目录或当前机器配置 |
| rembg CLI | `MIAOHUI_REMBG_PATH` / `imageTools.rembgPath` | 可选；优先复用配置 Python 的 rembg API |
| rembg 模型 | `MIAOHUI_REMBG_MODELS` / `imageTools.rembgModelsPath` | `.runtime/models/rembg` |
| Real-ESRGAN | `MIAOHUI_REALESRGAN_PATH` / `imageTools.realEsrganPath` | `.runtime/tools/realesrgan-ncnn-vulkan/realesrgan-ncnn-vulkan.exe` |
| Real-ESRGAN 模型 | `MIAOHUI_REALESRGAN_MODELS` / `imageTools.realEsrganModelsPath` | `.runtime/models/realesrgan-ncnn-vulkan` |
| 图片并发 | `MIAOHUI_IMAGE_CONCURRENCY` | `1`，最大 `2`；GPU 模型仍经共享闸门串行 |
| 画布存储键 | `src/data/seed.ts` | `miaohui-canvas:v8`；v7 只作迁移输入 |
| 像素模式草稿键 | `src/ProductApp.tsx` | `aeonquill:pixel-document:v1`；保存严格 schema v1，解析失败时安全回退到示例文档 |

当前本机已配置 H3 UNet、Qwen3-VL 编码器、视频/音频 VAE、Turbo LoRA、`u2netp.onnx` 和 Real-ESRGAN NCNN 便携模型。模型来源与校验值记录在 `.runtime/models/model-manifest.json`。没有前端 API 密钥；未来第三方密钥只能进入服务端秘密存储，不得使用 `VITE_*` 暴露。

## 10. 开发操作

### 10.1 安装与统一启动

```powershell
npm install
Copy-Item config/local.example.json config/local.json
npm run app
```

`npm run app` 先构建，再启动常驻桥接并在 `http://127.0.0.1:8787` 提供生产页面。默认 `idle` 策略不会立即启动 ComfyUI；提交视频任务或点击“启动”后才冷启动，并在本地 Job 与 ComfyUI 队列均为空达到阈值时关闭光阴砚拥有的子进程。外部启动的 ComfyUI 只复用、不关闭。用启动终端的 `Ctrl+C` 关闭桥接及其仍在运行的自有子进程。

桌面实验构建使用 `npm run package:desktop:electron` 或 `npm run package:desktop:tauri`。Tauri 命令会重建生产前端和自包含 Node 22 sidecar，再加载项目级 Rust/MSVC 环境生成 release 与 NSIS；完整路径和前置条件见[桌面封装与发行架构第 6 节](01-子文档/13-桌面封装与发行架构.md#6-构建与验证)。

### 10.2 开发模式

```powershell
# ComfyUI 已可用，终端 1
npm run server

# 终端 2
npm run dev
```

Vite 只监听 `127.0.0.1`，页面位于 `http://127.0.0.1:5173`。

### 10.3 验证

```powershell
npm run validate:quick   # 语法、75 项单元/离线/画布/项目存储/调度/桌面/预览缓存/安全契约、TypeScript
npm run validate         # 默认安全基线，不启动 ComfyUI
npm run validate:live    # 默认基线 + 已在线 ComfyUI 的实时 H3 契约
npm run validate:semantic-api  # 本机 Impact SAM 真实点击分割；会按需启动 ComfyUI
npm run validate:masked-edit   # 隔离桥接中的双输入 Mask API、SSE、来源和 PNG 输出
npm run benchmark:canvas -- --label=desktop --viewport=desktop --assert
npm run benchmark:canvas -- --label=mobile --viewport=mobile --assert
npm run validate:desktop:release  # 已有桌面产物：sidecar、两壳、NSIS 安装卸载与三轮资源对照
```

`validate:quick` 用于高频反馈。`validate` 还会构建 `dist/`、真实运行五种 FFmpeg 处理器，并在随机端口和系统临时目录启动桥接，验证 API、SSE、幂等图片 Job、优先级/资源/用量事件、输出版本 SHA-256、PNG 下载、Range 206、SQLite 原子项目保存、项目资产读取、`.miaohui` 下载/导入、版本化画布持久化、拖拽单修订、撤销重做、桌面 1440×900 与移动端 390×844 交互；它把 ComfyUI 策略固定为 `manual` 且指向不可达随机端口，因此不会冷启动模型，也不会改写正式 `.runtime` 数据。`validate:live` 仅在 ComfyUI 已在线时使用，会从实时 `/object_info` 检查 H3 节点必填输入与模型文件。

每次总入口运行都会把机器可读结果写入 `.runtime/qa/latest.json`。可用 `npm run validate:api`、`npm run validate:browser`、`npm run validate:image-tools`、`npm run validate:workflows`、`npm run validate:semantic-api` 和 `npm run validate:masked-edit` 单独定位；语义烟测使用系统临时目录隔离 Job/资产，但会真实冷启动本机 ComfyUI 与 SAM，默认成功后清理，可用 `AEONQUILL_KEEP_SEMANTIC_SMOKE=1` 保留结果供人工检查；蒙版区域烟测不启动 ComfyUI，固定验证 Mask 必填、公开 Job 脱敏、SSE 完成和 PNG 输出。性能命令另写 `.runtime/qa/canvas-performance-<label>.json` 并拒绝使用陈旧 `dist`。桌面专项报告分别写入 `.runtime/qa/desktop-*-final.json`，不会进入默认门禁，因为安装器测试会短暂写入当前用户卸载注册项。失败的浏览器烟测保留临时截图与隔离运行时，成功后自动清理。Windows 默认调用已安装的系统 Edge，可用 `MIAOHUI_BROWSER_CHANNEL` 显式覆盖。

## 11. 扩展指南

### 11.1 新增画布元件

1. 在 `ElementKind`、`CanvasElement`、`canvasCore.mjs` 的元素/字段白名单登记类型与字段。
2. 在 `CanvasElementView` 增加渲染，在 `Inspector.kindIcons` 登记图标。
3. 如可创建，接入 `ToolId`、`ToolRail` 和 `App.createAt()`。
4. 如需 Agent 操作，单独审查其可创建类型或可修改字段；不要默认开放媒体、资产与任务字段。
5. 验证选择、移动、缩放、复制、删除、撤销、本地迁移和非法命令拒绝。

### 11.2 新增视频参数或工作流版本

1. 先在 `validateVideoRequest()` 建立类型、范围和白名单。
2. 在 `workflow-builder.mjs` 映射到固定节点；不要接受前端节点 ID 或任意图。
3. 模板变化时保持节点阶段映射一致，或同步修改 `stageForNode()`。
4. 更新运行时必需节点/模型检查。
5. 运行 `npm run validate:workflows`，再做一条真实最小生成。
6. 同步[本地 H3 视频工作流](01-子文档/11-本地H3视频工作流.md)。

### 11.3 接入第三方 API 或 Agent

只能新增服务端 Adapter 和受控工具，不得从 React 组件直接调用带密钥的模型。实施前必须在规划中定义资产版本、费用、内容审核、超时、幂等和重试语义。Agent 只能生成经 `compileCanvasToolInvocation()` 校验的工具调用，并先用 `createCanvasDraft()` 形成预览；只有当前 revision 未变化时才能 `commitCanvasDraft()`。Agent 不能调用 `elements.replace`/`camera.set`、写任意画布 JSON 或提交任意 Comfy 工作流。

### 11.4 新增本地图像处理器

1. 在 `src/types.ts` 的 `ImageOperationId`、`server/image-processor.mjs` 的能力清单和 `imageOperationLabels` 同时登记稳定操作 ID。
2. 在 `validateImageRequest()` 只接受明确参数键、枚举和值域；按输入 20MB/40MP、输出 64MP 的现有边界拒绝超限任务。
3. 处理器必须把输出写到调用方指定的 `.runtime/assets` 路径，监听 `AbortSignal` 并终止完整子进程树；错误消息不得暴露本机绝对路径。
4. 使用 GPU 的操作加入 `gpuImageOperations`；所有正式处理器必须由 `JobScheduler` 分配资源并使用其 `AbortSignal`，不得从路由直接启动执行函数。CPU/FFmpeg 操作受 CPU 容量限制，GPU 与 H3 共用容量 1。
5. 在 `ImageLab` 登记参数、能力解释和可用/降级行为；如果没有真实处理器，正式按钮必须禁用，不得返回假结果。
6. 扩展 `server/validate-image-tools.mjs` 或增加最小真实烟测；验证能力探测、成功、非法参数、取消、失败建议、资产下载和画布派生回填。
7. 发布前分别登记代码许可证、模型权重许可证、来源、校验值和再分发条件；运行期下载不得发生在首个用户任务中。

### 11.5 新增 ComfyUI 语义图片工作流

1. 先在 `server/semantic-workflow-registry.mjs` 分配稳定 ID 与独立语义版本，登记扩展、模型、节点、输入、输出、显存和超时；`implemented: false` 的能力只能展示 `template-pending`，不能进入执行路由。
2. 前端只能提交产品参数，不能提交 ComfyUI 节点图、节点 ID 或本机路径；请求必须用精确键白名单、数量和值域校验后再持久化。
3. ComfyUI 节点图型工作流必须通过既有节点白名单；扩展 HTTP API 适配器必须固定端点、模型名、受管上传引用和响应类型，并支持 `AbortSignal`。
4. 正式结果必须保留原图，输出新的不可变图片版本；蒙版、深度或控制图如后续会参与编辑，也必须使用独立受管资产而不是只留在临时内存。
5. GPU 工作流进入 `JobScheduler` 的共享 GPU 容量 1；启动、进度、错误、取消、重试和空闲关闭继续复用统一 Job 语义。
6. 至少增加离线注册表/非法参数契约和一条真实本机最小烟测；模型许可与固定素材质量门禁完成前只能标为 preview/experimental，不得宣传为生产级编辑质量。

## 12. 测试与验证

截至 2026-08-14 已完成以下自动回归与真实集成验证：

| 检查 | 结果 |
| --- | --- |
| 一键安全基线 | `npm run validate` 通过 34 个分层检查；默认不启动 ComfyUI，不改写正式运行数据 |
| 单元与离线契约 | 75/75 通过：画布命令、空间索引、桌面桥接契约、图片预览缓存、SQLite 项目、调度/磁盘、Job 持久化/幂等、图片参数、视频与安全契约 |
| 本地桥接安全 | 无会话 401、恶意 Origin/Fetch Metadata 403、恶意 Host 421、合法开发源预检 204、资产穿越 403；CSP、COOP/CORP、Permissions-Policy、nosniff、DENY 与 HttpOnly Cookie 可查 |
| 崩溃恢复 | 模拟桥接重启：运行中任务变为可操作的 `BRIDGE_RESTARTED` 且未提交输出被清理，排队任务保留并由调度器恢复；已完成 Job 与正式资产保持不变 |
| 隔离 API | 健康、调度快照、项目/原图/版本化 WebP 缩略与预览/`.miaohui`、SSE、真实像素化 Job 均通过；同幂等键重复请求只返回原 Job，输出字节 SHA-256 与 `outputVersion.assetId` 相同，PNG/Range/清理与 404 通过 |
| 画布性能 | 300 轻节点 + 30 个真实 3840×2160 图片节点：桌面/移动帧 P95 均 4.3ms，指针反馈 P95 4.8/4.4ms，平移后挂载 46/27 个画布节点，总 DOM 348/279，受管原图解码为 0；全部硬断言通过 |
| 桌面壳生命周期 | Electron packaged 20 项、Tauri release 22 项断言通过；两者渲染器均无 Node/Shell 全局，桥接均正常退出且端口关闭 |
| Tauri 安装卸载 | 27.4 MiB 未签名 NSIS 在隔离当前用户目录安装 3.085s，已安装应用完成真实生命周期，卸载与注册项/快捷方式清理 0.498s |
| 桌面壳对照 | 三轮交替启动：Electron/Tauri 窗口加载中位数 0.857/1.446s，私有内存 434.4/466.3 MiB，运行文件 348.8/96.2 MiB；当前以 Tauri 为分发候选、Electron 为回退 |
| 隔离浏览器 | 系统 Edge 1440×900 验证画布修订/撤销重做，并真实展示 CPU、优先级、尝试次数、本机算力和不可变输出；派生结果规范化为项目资产后下载/重新导入 `.miaohui`。390×844 无横向溢出，控制台无 warning/error |
| 依赖审计 | `postcss` 8.5.26、`nanoid` 3.3.18；`npm audit` 为 0 项已知漏洞 |
| 工作流契约 | 文生 15 节点；含首末帧、音频关闭的图生组合 15 节点；720P 交付组合 15 节点；必填输入和五个模型文件通过 |
| TypeScript | `npm run typecheck` 通过 |
| 生产构建 | `npm run build` 通过 |
| 图片能力 | 7 项全部可用：5 项 FFmpeg、rembg/u2netp、Real-ESRGAN NCNN；`npm run validate:image-tools` 通过 |
| 确定性图片 | Lanczos 1881×1881、像素化 128×128、锐化 1254×1254、透明边清理 1254×1254、Mask 区域调整 1254×1254 均真实生成；像素化重复输出 SHA-256 一致 |
| 去背景 | API 小图 256×256 与浏览器原图 1254×1254 均完成，输出透明 PNG；执行器 `rembg` |
| Real-ESRGAN | Vulkan 小图 2× 输出 512×512；浏览器原图 2× 输出 2508×2508；执行器 `realesrgan-ncnn-vulkan` |
| 图片边界 | 未知参数 400、缺失适配器 409、取消后无 FFmpeg 残留进程；公开 Job 不泄露本机输入/输出路径 |
| SAM 元素提取 | 四类语义工作流注册表与 6 项离线契约通过；真实样例经按需 ComfyUI 冷启动、Impact SAM 点击分割、透明 PNG/灰度蒙版双输出、不可变版本和空闲关闭完成，公开能力不泄露扩展/模型绝对路径 |
| 语义区域调整 | SAM 派生图 Alpha 经双输入 `masked-adjust` 完成背景压暗并产生独立资产版本；缺 Mask 400、未知参数 400、SSE、取消与保留双输入重试通过，公开 Job 不含输入/Mask 路径；桌面与 390×844 重载恢复且控制台无 warning/error |
| 资源调度 | 高优先级稳定排序；CPU/GPU 可并行但各自遵守容量，GPU 严格单并发；排队/运行取消、1 秒故障注入超时、恢复和低磁盘 507 语义通过 |
| 成本与版本 | 本机 estimate/usage 事件金额固定为 0 且 `billable=false`；完成输出具有确定性版本 ID、内容 SHA-256 与 provenance |
| 文生真实生成 | 608×352、124 帧、4 步、原生音频，浏览器读取 5.167 秒 |
| 图生真实生成 | 首帧 1256×1256 cover 为 608×352 PNG；4 步完成，浏览器读取 5.167 秒 |
| 进度 | 真实采样 1/4、3/4、4/4 和解码阶段均被 API/前端观察到 |
| 资产 | MP4 Range 请求返回 206 和正确 Content-Range |
| 取消 | 创建后取消；Job 变为 cancelled，Comfy 等待/运行队列均为 0 |
| 生命周期 | 休眠到 RTX 4060 就绪约 38.7 秒；30 秒测试阈值下队列为空后自动关闭并恢复 300 秒配置 |
| 720P 交付 | 现有 5 秒 MP4 经 FFmpeg Lanczos 输出 1280×720，保留音频 |
| 桌面浏览器 | 既有视频闭环；本轮 1280×720 真实提交去背景与 Real-ESRGAN，任务显示尺寸/执行器/KB/下载并自动回填派生节点 |
| 移动浏览器 | 390×844 属性抽屉与图片实验室无页面级横向溢出，参数区可纵向滚动 |
| 控制台 | 最终无 app error/warning，无框架错误覆盖层 |

仓库使用 Node Test Runner，浏览器烟测通过 `@playwright/test` 提供的 Playwright 与系统 Edge 执行；另以浏览器控制层独立核验真实 8787 页面，未发现破图、横向溢出或控制台 warning/error。真实视频产物和任务记录属于 `.runtime`，不提交源码仓库。

## 13. 维护与故障排查

| 表现 | 诊断 | 处理 |
| --- | --- | --- |
| 视频页显示 ComfyUI 休眠 | 请求 `/api/health` 与 `/api/runtime/status?refresh=1` | 正常待机状态只要求 8787 存活；提交任务后再观察 8188 冷启动，或点击“启动”查看错误 |
| 组件未就绪 | `missingNodes` 或 `missingModels` 非空 | 运行 `npm run validate:workflows`，安装或登记准确节点/模型 |
| `[Errno 22] Invalid argument` | traceback 在 Turbo `print()` → logger `flush()` | 旧 Comfy 终端句柄失效；关闭旧进程并用统一启动器启动 |
| `COMFY_OOM` | CUDA OOM 或分配失败 | 快速预设、5 秒、释放显存；再看 `.runtime/comfyui.log` |
| 图片能力显示未安装 | `GET /api/image-tools?refresh=1` 检查 `unavailableReason` | 核对 `.runtime/models/model-manifest.json`、模型文件、可执行文件和 `config/local.json`；重启桥接后刷新 |
| 元素提取显示不可用 | `GET /api/semantic-workflows?refresh=1` 检查 `status`、`missingExtensions`、`missingArtifacts`、`missingNodes` | 核对 Impact Pack、`models/sams/sam_vit_b_01ec64.pth` 和 `SAMLoader` 注册；不要把 `runtime-stopped` 误判为缺失，正式提交会按需启动 |
| SAM 未返回稳定蒙版 | Job 错误为 `SEMANTIC_MASK_NOT_FOUND`，或蒙版粘连/缺口明显 | 把正向点放在主体内部、背景增加负向点并调低阈值；再用蒙版修边，不要覆盖原图 |
| 去背景首次执行尝试下载 | 运行器或模型目录被绕开 | 必须走 `server/rembg-runner.py`；其固定白名单与 MD5 校验会拒绝网络下载和目录逃逸 |
| Real-ESRGAN 输出失败 | 检查模型 `.param/.bin` 是否成对、模型名/scale/tile | 先用 `realesr-animevideov3`、2×、tile 192；再看图片 Job 日志 |
| 图片模型长时间等待 | `GET /api/scheduler` 显示 GPU 容量正在服务 H3 或另一图片模型 | 在任务卡核对资源类别/优先级并可取消等待；不要绕过调度器直接启动执行器 |
| 外部 ComfyUI 占用显存 | 图片任务日志出现外部进程警告 | 妙绘不会关闭用户手动进程；用户确认无任务后自行关闭或接受显存风险 |
| 页面任务与 Comfy 不一致 | 检查 Prompt ID、`/history/:id`、`/api/scheduler` 和 `.runtime/jobs.json` | 运行中 Job 在桥接重启后安全失败，排队 Job 自动恢复；检查外部 Comfy 队列后再决定重试，避免重复输出 |
| `STORAGE_CAPACITY_LOW` / `STORAGE_EXHAUSTED` | 检查 `.runtime` 所在卷可用空间 | 先释放空间；预检失败不会创建 Job，执行期写满不会提交正式输出 |
| 视频不能拖动播放 | 检查 `/api/assets/...` Range 是否返回 206 | 不绕过 `serveVideoAsset()`；确认资产仍存在 |
| 图生主体变形 | 检查 `.runtime/inputs/<job>.png` 尺寸 | 必须走 `normalizeVideoFirstFrame()`，不可直传原图 |
| 一次拖动需多次撤销 | 检查是否在手势移动中调用 `commitElements()` | 保持预览/提交两阶段 |
| 浏览器去背误伤白色主体 | 当前选择了浏览器草稿 | 切换“本机正式处理”使用 u2netp；细发丝/半透明仍需后续更高质量 Matting 基准 |
| 自动保存或项目重开失败 | 检查 `/api/projects/current`、SQLite 文件权限、磁盘余量和浏览器恢复副本 | 保留 `localStorage` 恢复副本；修复 `.runtime/projects` 权限/空间后重试，禁止直接编辑数据库或覆盖内容寻址资产 |
| 画布图片破图或回退原图 | 检查图片响应的 `x-miaohui-asset-tier`、`?variant=` 和 FFmpeg 可用性 | 运行 `npm run validate:api`；派生缓存可删除后重建，但不得删除 `.runtime/projects/assets` 原始内容资产 |
| 桌面窗口未出现 | 查看对应 `.runtime/qa/desktop-*-final.json` 的 bridge/renderer timeline | 先运行 sidecar 或壳专项验证；桥接健康前窗口不会显示，禁止绕过健康检查直接加载页面 |
| Tauri 构建找不到 Rust/MSVC/NSIS | 检查 `.runtime/toolchains/rust`、VS 2022 C++ 工具和 Tauri 本地缓存 | 使用 `scripts/tauri-toolchain.ps1 check`，不要临时改全局 PATH 或跳过下载哈希 |
| 卸载后项目是否保留不明确 | 当前实验卸载器只验证程序、注册项和快捷方式 | 正式卸载必须默认保留用户项目，并单独提供清缓存/模型/项目选项；进入 `REL-002` 前不得改变此原则 |

统一启动器管理的 Comfy 日志在 `.runtime/comfyui.log`；Comfy 自己也可能写入其 `user/comfyui.log`。排错时优先查看任务错误的节点 ID、类型和 Prompt ID，再关联日志时间。

## 14. 关联文档

- [00-项目总纲.md](00-项目总纲.md)
- [01-子文档/11-本地H3视频工作流.md](01-子文档/11-本地H3视频工作流.md)
- [01-子文档/12-无限画布性能与资源分级.md](01-子文档/12-无限画布性能与资源分级.md)
- [01-子文档/13-桌面封装与发行架构.md](01-子文档/13-桌面封装与发行架构.md)
- [02-项目规划.md](02-项目规划.md)
- [03-开发历史.md](03-开发历史.md)
